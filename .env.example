TIKTOK_USERNAME=kathyuyen.ta
PORT=3000
HOST=127.0.0.1
ALLOW_REMOTE_ACCESS=false
# Development uses the Render backend through a localhost proxy.
# Copy the APP_AUTH_TOKEN value from the Render service environment.
APP_AUTH_TOKEN=
REMOTE_BACKEND_URL=https://tiktoklivetracking-api.onrender.com
DATABASE_URL=
API_BASE_URL=
SOCKET_URL=
# Comma-separated origins accepted by the remote backend.
FRONTEND_ORIGIN=
# Dashboard login. Store only an Argon2id hash, never a plaintext password.
AUTH_USERNAME=
AUTH_PASSWORD_HASH=
