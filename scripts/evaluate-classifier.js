import { classifyQuestion } from "../src/question-detector.js"; import { developmentCorpus,holdoutCorpus } from "../test/fixtures/classifier-corpus.js";
function evaluate(rows){let tp=0,tn=0,fp=0,fn=0;const byTopic={};for(const row of rows){const actual=classifyQuestion(row.text).question;if(row.expected&&actual)tp++;else if(row.expected)fn++;else if(actual)fp++;else tn++;const item=byTopic[row.topic]||={total:0,correct:0};item.total++;item.correct+=actual===row.expected?1:0}const precision=tp/(tp+fp)||0,recall=tp/(tp+fn)||0;return{cases:rows.length,tp,tn,fp,fn,precision:+precision.toFixed(3),recall:+recall.toFixed(3),f1:+(2*precision*recall/(precision+recall)||0).toFixed(3),perTopic:Object.fromEntries(Object.entries(byTopic).map(([k,v])=>[k,+(v.correct/v.total).toFixed(3)]))}}
console.log(JSON.stringify({development:evaluate(developmentCorpus),holdout:evaluate(holdoutCorpus),note:"Synthetic template baseline; not LIVE validation"},null,2));
